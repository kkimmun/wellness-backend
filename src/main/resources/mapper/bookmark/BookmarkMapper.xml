<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.wellness.bookmark.model.dao.BookmarkMapper">

	<select id="countActivePlace" resultType="int">
		SELECT
			COUNT(*)
		FROM
			PLACE
		WHERE
			PLACE_NO = #{placeNo}
		AND
			DEL_YN = 'N'
	</select>

	<select id="countBookmark" resultType="int">
		SELECT
			COUNT(*)
		FROM
			PLACE_BOOKMARK
		WHERE
			MEMBER_NO = #{memberNo}
		AND
			PLACE_NO = #{placeNo}
	</select>

	<insert id="insertBookmark">
		INSERT INTO
			PLACE_BOOKMARK (
				MEMBER_NO
				,PLACE_NO
				,CREATE_DATE
			)
		VALUES (
			#{memberNo}
			,#{placeNo}
			,SYSDATE
		)
	</insert>

	<delete id="deleteBookmark">
		DELETE FROM
			PLACE_BOOKMARK
		WHERE
			MEMBER_NO = #{memberNo}
		AND
			PLACE_NO = #{placeNo}
	</delete>

</mapper>
