<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.wellness.course.model.dao.CourseMapper">

    <select id="selectActiveCourses"
            resultType="com.kh.wellness.course.model.dto.CourseListRow">
        SELECT
            C.COURSE_NO,
            C.COURSE_NAME,
            START_PLACE.PLACE_NO AS START_PLACE_NO,
            START_PLACE.PLACE_NAME AS START_PLACE_NAME,
            END_PLACE.PLACE_NO AS END_PLACE_NO,
            END_PLACE.PLACE_NAME AS END_PLACE_NAME,
            C.ESTIMATED_TIME,
            C.DESCRIPTION
        FROM
            COURSE C
        JOIN PLACE START_PLACE
            ON START_PLACE.PLACE_NO = C.START_PLACE
        JOIN PLACE END_PLACE
            ON END_PLACE.PLACE_NO = C.END_PLACE
        WHERE
            C.ACTIVE = 'Y'
        ORDER BY
            C.COURSE_NO DESC
        OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
    </select>

    <select id="countActiveCourses" resultType="long">
        SELECT
            COUNT(*)
        FROM
            COURSE C
        JOIN PLACE START_PLACE
            ON START_PLACE.PLACE_NO = C.START_PLACE
        JOIN PLACE END_PLACE
            ON END_PLACE.PLACE_NO = C.END_PLACE
        WHERE
            C.ACTIVE = 'Y'
    </select>
    
    <select id="selectByCourseNo" 
    	    resultType="com.kh.wellness.course.model.dto.CourseResponse">
    	SELECT
    	       COURSE_NO
    	     , START_PLACE
    	     , END_PLACE
    	     , COURSE_NAME
    	     , ESTIMATED_TIME
    	     , DESCRIPTION
          FROM
               COURSE
         WHERE
               ACTIVE = 'Y'
           AND
               COURSE_NO = #{courseNo}
   	</select>
   	
   	<select id="selectWaypointBycourseNo"
   			resultType="com.kh.wellness.course.model.dto.WaypointDto">
   		SELECT
   		       W.WAYPOINT_NO
   		     , W.COURSE_NO
   		     , W.PLACE_NO
   		     , W.WAYPOINT_SEQUENCE
   		     , P.PLACE_NAME
   		     , P.xAxis
   		     , P.yAxis
   		     , PI.IMG_PATH || PI.SAVE_NAME AS imageUrl
   		  FROM
   		       COURSE C
   		  JOIN
   		       COURSE_WAYPOINT W ON C.COURSE_NO = W.COURSE_NO
   		  JOIN
   		       PLACE P ON W.PLACE_NO = P.PLACE_NO
   		  LEFT
   		  JOIN
   		       PLACE_IMG PI ON P.PLACE_NO = PI.PLACE_NO
   		 WHERE
   		       W.COURSE_NO = #{courseNo}
   		   AND
   		       C.ACTIVE = 'Y'
   		   AND
   		       P.DEL_YN = 'N'
   		 ORDER
   		    BY
   		       W.WAYPOINT_SEQUENCE ASC
    	</select>

</mapper>
