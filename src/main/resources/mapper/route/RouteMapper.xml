<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.wellness.route.model.dao.RouteMapper">

    <select id="findPlaceByNo"
            parameterType="long"
            resultType="Place">
        SELECT
                PLACE_NO,
                PLACE_NAME,
                TRIM(ADDR || ' ' || NVL(ADDR_DETAIL, '')) AS ADDRESS,
                X_AXIS,
                Y_AXIS
        FROM
                PLACE
        WHERE
                PLACE_NO = #{placeNo}
        AND
                DEL_YN = 'N'
    </select>

    <select id="findPlacesByQuery"
            resultType="Place">
        SELECT
                PLACE_NO,
                PLACE_NAME,
                TRIM(ADDR || ' ' || NVL(ADDR_DETAIL, '')) AS ADDRESS,
                X_AXIS,
                Y_AXIS
        FROM
                PLACE
        WHERE
                DEL_YN = 'N'
        AND
                INSTR(UPPER(PLACE_NAME), UPPER(#{query})) > 0
        ORDER BY
                CASE
                    WHEN UPPER(PLACE_NAME) = UPPER(#{query}) THEN 0
                    WHEN INSTR(UPPER(PLACE_NAME), UPPER(#{query})) = 1 THEN 1
                    ELSE 2
                END,
                PLACE_NAME,
                PLACE_NO
        FETCH FIRST 15 ROWS ONLY
    </select>

    <select id="findMapPlaces"
            resultType="MapPlace">
        SELECT
                P.PLACE_NO,
                P.PLACE_NAME,
                P.PLACE_DESCRIPTION,
                P.ADDR,
                P.ADDR_DETAIL,
                P.PHONE_NUMBER AS PHONE,
                T.TYPE,
                P.VIEW_COUNT,
                P.X_AXIS,
                P.Y_AXIS
        FROM
                PLACE P
        LEFT JOIN
                TYPE_DETAIL TD
        ON
                P.TYPE_DETAIL_NO = TD.TYPE_DETAIL_NO
        LEFT JOIN
                TYPE T
        ON
                TD.TYPE_NO = T.TYPE_NO
        WHERE
                P.DEL_YN = 'N'
        ORDER BY
                P.PLACE_NO
    </select>

</mapper>
